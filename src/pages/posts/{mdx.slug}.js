import React from "react";
import Layout from "../../components/Layout";

const Post = () => {
  return (
    <Layout pageTitle="Rad page">
      <p>Content will *eventually* be displayed here.</p>
    </Layout>
  )
};

export default Post;